// GENERATED from mobile/liblogos-smoke/modules/bare_counter/manifest.json.
//
// The manifest is compiled in rather than shipped as a file because there is
// nowhere to ship it TO: the Bare module image lives in <App>.app/Frameworks/
// on iOS and in the app's native library directory on Android, both of which
// are flat, read-only, and the only places their platform will dlopen from.
// The manifest therefore travels with the app -- see
// logos_core_add_bare_module.
#pragma once

inline constexpr const char* kBundledModuleManifest = R"LOGOSJSON(@LOGOS_BUNDLED_MANIFEST_JSON@)LOGOSJSON";
